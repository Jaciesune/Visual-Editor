<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VE.Views.MainPage"
             Title="Edytor graficzny">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- Główna treść -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Panel narzędzi -->
            <ColumnDefinition Width="*"/>
            <!-- Canvas i warstwy -->
            <ColumnDefinition Width="300"/>
            <!-- Panel warstw -->
        </Grid.ColumnDefinitions>

        <!-- Pasek narzędzi głównych -->
        <HorizontalStackLayout Grid.Row="0" Grid.ColumnSpan="3"
            Padding="4"
            Spacing="8"
            BackgroundColor="#23262B"
            HeightRequest="48">
            <Button Text="Nowy"
                TextColor="White"
                BackgroundColor="#393e46"
                FontAttributes="Bold"
                CornerRadius="6"/>
            <Button Text="Otwórz"
                TextColor="White"
                BackgroundColor="#393e46"
                FontAttributes="Bold"
                CornerRadius="6"/>
            <Button Text="Zapisz"
                TextColor="White"
                BackgroundColor="#393e46"
                FontAttributes="Bold"
                CornerRadius="6"/>
        </HorizontalStackLayout>

        <!-- Lewy panel narzędzi rysowania -->
        <StackLayout Grid.Row="1" Grid.Column="0" BackgroundColor="#525252">
            <Label Text="Narzędzia" FontAttributes="Bold" Margin="10"/>
            <!-- Placeholdery na narzędzia -->
            <Button Text="Pędzel"/>
            <Frame BorderColor="#393e46" Margin="8,16,8,8" Padding="8" BackgroundColor="#636363">
                <VerticalStackLayout Spacing="6">
                    <Label Text="Kolor pędzla (RGB):" FontAttributes="Bold" TextColor="#23262B"/>
                    <HorizontalStackLayout Spacing="4">
                        <Entry x:Name="RValue" WidthRequest="40" MaxLength="3"
                   Keyboard="Numeric" Placeholder="R"/>
                        <Entry x:Name="GValue" WidthRequest="40" MaxLength="3"
                   Keyboard="Numeric" Placeholder="G"/>
                        <Entry x:Name="BValue" WidthRequest="40" MaxLength="3"
                   Keyboard="Numeric" Placeholder="B"/>
                    </HorizontalStackLayout>
                    <BoxView x:Name="BrushPreview"
                 HeightRequest="30"
                 CornerRadius="5"
                 Margin="2"
                 WidthRequest="70"
                 BackgroundColor="Black"/>
                </VerticalStackLayout>
            </Frame>
            <Button Text="Gumka"/>
            <Button Text="Wiadro"/>
            <Button Text="Pipeta"/>
        </StackLayout>

        <!-- Główny Canvas -->
        <Frame Grid.Row="1" Grid.Column="1" Margin="10" BackgroundColor="White" BorderColor="#AAAAAA" CornerRadius="4">
            <!-- Tutaj będzie SKCanvasView z kodu -->
            <Label Text="CANVAS (SKCanvasView tutaj)" VerticalOptions="Center" HorizontalOptions="Center" TextColor="#CCCCCC"/>
        </Frame>

        <!-- Panel warstw -->
        <StackLayout Grid.Row="1" Grid.Column="2" BackgroundColor="#525252" Padding="8">
            <Label Text="Warstwy" FontAttributes="Bold"/>
            <ListView>
                <!-- Placeholder -->
                <ListView.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>Tło</x:String>
                        <x:String>Warstwa 1</x:String>
                    </x:Array>
                </ListView.ItemsSource>
            </ListView>
            <Button Text="Dodaj warstwę" Margin="0,10,0,0"/>
        </StackLayout>
    </Grid>
</ContentPage>
